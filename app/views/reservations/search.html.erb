<h4>空室確認</h4>
<div class="wrapper">
  <p>①宿泊する日付と人数を選択してください。</p>
  <p>※１泊の予約しかできません。</p>

<div id="date_errors">
  <%= render "reservations/date_errors", reservation: @reservation %>
</div>

  <%= form_with model: @reservation, url: room_reservations_path do |f| %>
    <div class="search">
      <div class="date">
        <label>チェックイン日</label><%= f.date_field :start_date, required: true %>
      </div>
      <div class="date">
        <label>チェックアウト日</label><%= f.date_field :end_date, required: true %>
      </div>
      <div class="date">
        <label>宿泊人数</label><%= f.select :people, [1,2,3,4] %>
      </div>
    </div>
    <%= f.submit "検索", class: "button" %>
  <% end %>
</div>

<div id="room-js">
</div>