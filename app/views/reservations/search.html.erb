<h4></h4>
<div class="wrapper">
  <%= render "reservations/step" %>
</div>
  <h5>空室確認</h5>
  <p>①宿泊する日付と人数を選択してください。</p>
  <p>※１泊の予約しかできません。</p>

<div class="wrapper">
  <div id="date_errors">
  </div>
  <%= form_with model: @reservation, url: room_reservations_path do |f| %>
    <div class="search">
      <div class="date">
        <label for="reservation_start_date">チェックイン日</label><%= f.date_field :start_date, required: true %>
      </div>
      <div class="date">
        <label for="reservation_end_date">チェックアウト日</label><%= f.date_field :end_date, required: true %>
      </div>
      <div class="date">
        <label for="reservation_people">宿泊人数</label><%= f.select :people, [1,2,3,4] %>
      </div>
    </div>
    <%= f.submit "検索", class: "button" %>
  <% end %>
</div>

<div id="room-js">
</div>

<script>
  $(function(){
    path = location.pathname
    if(path.match("/reservations/search")){
    $(".step1").addClass("step-now");
    }
  });
</script>
